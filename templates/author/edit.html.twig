{% extends 'base.html.twig' %}

{% block title %}Редактирование автора{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow-sm p-4">
            <h2 class="mb-4">Редактирование автора</h2>

            {{ form_start(form) }}
                <div class="mb-4">
                    {{ form_label(form.fullName, null, {'label_attr': {'class': 'form-label'}}) }}
                    {{ form_widget(form.fullName, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.fullName, {'attr': {'class': 'text-danger small'}}) }}
                </div>

                <div class="d-flex justify-content-between">
                    <a href="{{ path('app_author_index') }}" class="btn btn-outline-secondary">Отмена</a>
                    <button type="submit" class="btn btn-warning px-4">Сохранить изменения</button>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
